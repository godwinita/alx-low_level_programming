#include "main.h"

/**
 *print_alphabet - prints the alp[habet in lowercase, followed by a new line
*/

void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; lettetr++)
		_putchar(letter);

	_putchar('\n');
}

